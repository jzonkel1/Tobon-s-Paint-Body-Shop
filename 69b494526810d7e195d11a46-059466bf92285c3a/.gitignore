# dependencies
node_modules/

# build output
dist/
*.tsbuildinfo

# local env / secrets — never commit SMTP passwords or app passwords
.env
.env.*
!.env.example

# logs & misc
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.DS_Store
